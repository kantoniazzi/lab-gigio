<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      REDE: concedida, e a garantia mudou de natureza.

      Até a introdução da telemetria, o Gigio não declarava INTERNET, e isso
      tornava o vazamento *estruturalmente impossível*: o app não conseguia
      falar com a rede. Essa era a garantia mais forte que existia.

      Ao instrumentar com Datadog, a permissão passou a ser necessária. A
      garantia agora é **verificada, e não estrutural** — mais fraca, e a troca
      precisa estar registrada:

      1. A rede é usada SÓ pelo módulo lib/engines/telemetry/. Nenhum board,
         frase, rótulo ou foto atravessa essa fronteira.
      2. O app envia apenas ao Worker do Cloudflare, que revalida cada evento
         contra uma lista de campos permitidos antes de repassar ao Datadog.
      3. Página e botão só sobem com consentimento parental explícito, desligado
         por padrão.
      4. test/security_test.dart verifica 1 e 3 no build.

      Ver worker/src/index.js e docs/architecture.md.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="Gigio"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="sensorLandscape"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
